{
  "actorSpecification": 1,
  "fields": {
    "title": {
      "type": "string",
      "nullable": true,
      "description": "Job position title"
    },
    "company": {
      "type": "string",
      "nullable": true,
      "description": "Hiring company name"
    },
    "sector": {
      "type": "string",
      "nullable": true,
      "description": "Job sector or category"
    },
    "location": {
      "type": "string",
      "nullable": true,
      "description": "Job location (city, province, or remote)"
    },
    "salary": {
      "type": "string",
      "nullable": true,
      "description": "Salary information (monthly/annual in ZAR)"
    },
    "job_type": {
      "type": "string",
      "nullable": true,
      "description": "Employment type (Permanent, Contract, Temporary, etc.)"
    },
    "date_posted": {
      "type": "string",
      "nullable": true,
      "description": "Date when the job was posted"
    },
    "valid_through": {
      "type": "string",
      "nullable": true,
      "description": "Job listing expiration date"
    },
    "description_html": {
      "type": "string",
      "nullable": true,
      "description": "Full job description in HTML format"
    },
    "description_text": {
      "type": "string",
      "nullable": true,
      "description": "Plain text version of job description"
    },
    "url": {
      "type": "string",
      "nullable": false,
      "description": "Direct URL to the job listing"
    }
  },
  "views": {
    "overview": {
      "title": "Overview",
      "transformation": {
        "fields": [
          "title",
          "company",
          "sector",
          "location",
          "salary",
          "job_type",
          "date_posted",
          "url"
        ]
      },
      "display": {
        "component": "table",
        "properties": {
          "title": {
            "label": "Job Title",
            "format": "text"
          },
          "company": {
            "label": "Company",
            "format": "text"
          },
          "sector": {
            "label": "Sector",
            "format": "text"
          },
          "location": {
            "label": "Location",
            "format": "text"
          },
          "salary": {
            "label": "Salary",
            "format": "text"
          },
          "job_type": {
            "label": "Job Type",
            "format": "text"
          },
          "date_posted": {
            "label": "Posted",
            "format": "text"
          },
          "url": {
            "label": "URL",
            "format": "link"
          }
        }
      }
    }
  }
}
